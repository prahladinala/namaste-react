import React from "react";
import ReactDOM from "react-dom";
import "./styles.css";

const App = () => (
  <div>
    <h1>Hello, Parcel with React!</h1>
  </div>
);

ReactDOM.render(<App />, document.getElementById("root"));
